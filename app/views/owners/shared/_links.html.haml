.footer_btn_area
  - if controller_name != 'sessions'
    = link_to "店舗ログイン", new_session_path(resource_name), class: "btn btn-primary"

  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "店舗会員登録", new_registration_path(resource_name), class: "btn btn-primary"

  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "パスワードを忘れた方", new_password_path(resource_name), class: "btn btn-primary"

  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "店舗認証メールが届かない方", new_confirmation_path(resource_name), class: "btn btn-primary"

  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = link_to "アカウントがロックされた方", new_unlock_path(resource_name), class: "btn btn-primary"

  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider)
%br
